import 'package:flutter/material.dart';

class LegendSizeBox extends StatelessWidget {
  const LegendSizeBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
